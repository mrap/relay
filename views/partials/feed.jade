h1 Posts Browser

div(ng-controller="PostsController")
  div(feed public-feed='popular')
    div(ng-repeat='post in posts')
      .post
        .post-info
          span
            {{ post._last_relayed_by.username.toUpperCase() }}
          |  relayed a post
        |  Created {{ post.relay_count }} users away
